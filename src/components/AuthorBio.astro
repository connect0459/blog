---
import { SITE_DESCRIPTION, SITE_TITLE, type AuthorLink } from '../consts';

interface Props {
	links?: readonly AuthorLink[];
}

const { links } = Astro.props;
---

<section class="author-profile">
	<div class="author-avatar" aria-hidden="true"></div>
	<div class="author-name">{SITE_TITLE}</div>
</section>
<p class="author-bio">{SITE_DESCRIPTION}</p>
{
	links && (
		<div class="author-social">
			{links.map((link) => (
				<a href={link.href} target="_blank" rel="noopener noreferrer">
					{link.label} ↗
				</a>
			))}
		</div>
	)
}
<style>
	.author-profile {
		display: flex;
		align-items: center;
		gap: var(--s-3);
	}
	.author-avatar {
		width: 48px;
		height: 48px;
		flex-shrink: 0;
		background: var(--surface-container);
	}
	.author-name {
		font-size: 16px;
		font-weight: var(--fw-medium);
	}
	.author-bio {
		margin: var(--s-4) 0 0;
		max-width: 480px;
		font-size: 15px;
		line-height: 1.85;
		color: var(--secondary);
	}
	.author-social {
		display: flex;
		gap: 20px;
		margin-top: var(--s-3);
		font-size: var(--fs-label);
		letter-spacing: 0.06em;
		color: var(--secondary);
	}
</style>
